# Optional: configure both to enable Supabase authentication and persistence.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server only; used by the seed command, never sent to a browser.
SUPABASE_SERVICE_ROLE_KEY=
# Mock checkout remains explicitly simulated; no real charges.
PAYMENT_PROVIDER=mock
